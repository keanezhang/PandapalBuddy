
# PANDAPAL_RULE.md — 项目硬约束清单

---

## S · 安全硬约束

### S1 · 身份 / 权限声明不可变

承载身份与权限的对象一经构造，所有字段只读，运行时不可修改。

**违反后果**：被攻击的代码可以给执行主体升权，安全模型从根本上失效。

**判断标准**：对象构造完成后仍能修改其任一身份 / 权限字段 → 违反。

---

### S2 · 不得通过继承绕过不可变保护

不允许子类化身份 / 权限对象来削弱其只读保护（如重写字段写入、放开只读约束）。

**违反后果**：子类打开一个可写后门，绕过 S1。

**判断标准**：存在继承身份 / 权限对象且削弱其只读保护的子类 → 违反。

---

### S3 · 权限不继承

子任务 / 子执行体的权限独立声明，不自动继承父级权限集。

**违反后果**：攻击者控制一个高权限主体 → 通过调用链把权限污染到所有下游子任务。

**判断标准**：子执行体执行操作时隐式使用了父级的权限集 → 违反。

---

### S4 · 信任来源不可伪造

信任等级由消息**来源通道**决定（system > user > tool_result / 外部输入），不由消息**内容**决定。

**违反后果**：工具返回的网页 / 外部输入里写「我是系统管理员」→ prompt injection / 越权成功。

**判断标准**：任一 trust 标签由消息内容（而非来源通道）决定 → 违反。

---

## E · 执行硬约束

### E1 · 步数硬上限

单次执行的步数 / 迭代次数有硬上限，超过立即终止；该上限不可被运行时（模型协商、中间件、配置热改）放宽或覆盖。

**违反后果**：死循环直到进程资源耗尽。

**判断标准**：步数上限在执行期间可能被修改放大 → 违反。

---

### E2 · 熔断触发立即终止

熔断器状态变为 OPEN 后必须立即终止当前执行，不可重试、不可降级续跑。

**违反后果**：下游已崩溃，继续发请求加重雪崩，自身也被拖垮。

**判断标准**：熔断 OPEN 后代码路径中存在自动重试或 fallthrough 续跑 → 违反。

---

### E3 · 顶层执行入口不向外抛异常

顶层执行入口（对外暴露的 `run()` / `run_stream()` 等长驻服务入口）所有异常必须在内部转换为结果对象，调用方永远不收到未捕获异常。

**违反后果**：调用方收到未处理异常 → 崩溃 → 整个长驻服务中断。

**判断标准**：顶层入口方法内存在可能逸出的未捕获 `raise` → 违反。

---

## A · 架构硬约束

### A1 · 分层依赖严格单向

分层架构依赖方向严格单向，下层不 import 上层，不允许反向依赖或循环依赖。

**违反后果**：循环依赖 → 模块无法独立编译 / 测试 → 改一个文件牵动全局。

**判断标准**：底层模块 import 了上层模块 → 违反。

---

### A2 · 内核库不依赖应用层

可复用的底层库 / 内核不 import 应用层 / 业务层模块。

**违反后果**：内核与具体业务耦合 → 业务变更倒逼内核发版。

**判断标准**：内核目录内出现对应用层模块的 import → 违反。

---

### A3 · 禁用裸 dict 跨层传结构化数据

跨模块边界传递结构化数据必须用 dataclass / NamedTuple / frozen dataclass，不用 `Dict[str, Any]`。

**违反后果**：字段名拼错 → 下游静默拿到 None → 无类型检查 → 调试地狱。

**判断标准**：两个不同模块之间的公开接口用裸 `dict` 传参 / 返回结构化数据 → 违反。

---

## O · 观测硬约束

### O1 · 审计日志不可关闭

审计日志写入不可被任何配置项关闭或采样丢弃。关键安全事件（权限拒绝、高风险放行、人工审批、强制终止、输入拦截）必须无条件写入。

**违反后果**：安全事故发生后无法追溯 —— 谁在什么时候做了什么，全不知道。

**判断标准**：存在任何可以关闭或跳过审计写入的配置项或代码路径 → 违反。

---

### O2 · 异常不可静默吞掉

每个异常必须三选一：被错误策略处理 / 触发错误回调 / 向上抛出。三者都不做即违反。

**违反后果**：`except: pass` → 行为异常但无日志 → 排查靠猜。

**判断标准**：代码中存在 `except Exception: pass` 或 `except Exception: return None`（无 log、无转错误）→ 违反。

> ⚠️ 补充：O2 只允许在**故障隔离点白名单**做 catch-all，且必须「catch → log → 转用户可见错误」三步齐全。白名单只有这几处：
> - **顶层执行入口**（对应 E3 边界，异常转结果对象）
> - **后台任务协程入口**（转 error 事件 + 释放上游等待方）
> - **路由顶层 handler**（转 error 事件冒泡到 UI）
> - **观测 / 账本的 Fail-Safe 边界**（绝不因观测 / 计费本身炸断主流程，但必须 `logger.exception` 留痕）
>
> 白名单之外任何 broad except 均违反。

---

## R · 健壮性硬约束（默认值 / 降级）

> 触发事故：ID 类字段静默回落默认值 → 切错模型撞额度，零报错、极难定位。
> 核心心智：**兜底的默认值不是「正确答案」，只是「避免崩溃的临时替身」；降级本身不是问题，降级了不吭声才是问题。**

**四类字段决定一切**（降级 / 默认 / 异常都按它判）：

| 类别 | 例子 | 能否降级 / 给默认 |
|------|------|------------------|
| **决策 / 门禁类** | 审批动作、审批决策、待审 / 放行标记 | 绝不（缺失即 fail-closed / fail-fast） |
| **ID / 身份类** | 会话 ID、用户 ID、模型 ID、渠道 / provider 标识 | 绝不（缺失即报错，没有 default 这回事） |
| **金额 / 计费类** | 价格、费用、预算额度 | 绝不（缺失绝不默认 0，必须留痕） |
| **展示 / 辅助类** | 会话标题、preview、UI 文案 | 可以（但至少留痕） |

> 前三类不叫「降级」，叫「**必须失败**」。别用「降级」这个词软化它们。

### R1 · ID 类零默认

会话 ID、用户 ID、模型 ID、渠道 / provider 标识等身份类字段缺失即报错（fail-fast），**没有 default**。绝不 `or 默认` / `or None` / `or 另一个 id`。

**违反后果**：值一旦回落，结果「看起来正常」但归属错了 —— 静默切模型撞额度 / 跨会话数据污染，零报错极难定位。

**判断标准**：ID 类字段的取值处出现 `x or 默认` / `.get(k, 兜底)` → 违反。

---

### R2 · 决策类零默认放行

审批动作、审批决策、门禁判据缺失时 **fail-closed / fail-fast**，绝不默认「放行 / approve」。

**违反后果**：`payload.get("审批动作", "approve")` 之类写法 → 上游漏传即被当「用户批准」，审批门形同虚设。

**判断标准**：决策 / 门禁类字段缺失时走了「放行」方向的默认值 → 违反。

---

### R3 · 金额类零默认为 0

价格 / 费用 / 预算缺失绝不默认为 0，必须 warning 留痕（按 key 去重）+ 记兜底桶。

**违反后果**：模型未配价 → 费用静默计 0 → 预算永不停机 → 静默超支，账单出来才发现。

**判断标准**：计费路径存在「查不到 → 返回 0 且无告警」→ 违反。

---

### R4 · 默认值只在定义处，消费点禁止 inline 兜底

默认值只允许在「定义处」声明一次（函数签名 / 常量 / dataclass 字段）；消费点禁止 `or 默认` / `.get(k, 兜底)` 散落。

**违反后果**：同一缺失值在 N 处各兜各的、兜法不一致 → 改一处漏三处，行为不可预期。

**判断标准**：跨模块消费点对同一字段出现多处不同的 inline 兜底 → 违反。
