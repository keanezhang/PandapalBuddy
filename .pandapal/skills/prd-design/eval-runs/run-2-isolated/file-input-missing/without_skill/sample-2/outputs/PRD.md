# PRD — 产品需求文档

> **状态说明**：本 PRD 无法基于源需求文件生成。源文件 `C:\nonexistent\requirements.md` 不存在，未提取到任何需求内容。为避免凭空编造需求细节，本文档不包含虚构的产品功能描述，仅如实记录输入缺失的情况及后续处理所需的信息。

## 1. 输入文件状态

| 项目 | 内容 |
| --- | --- |
| 请求读取的源文件路径 | `C:\nonexistent\requirements.md` |
| 读取结果 | 文件不存在 |
| 原因 | 路径中的目录 `C:\nonexistent` 不存在，因此该文件无法被读取 |

## 2. 无法生成完整 PRD 的原因

用户请求为「读取 `C:\nonexistent\requirements.md` 并基于其内容生成一份 PRD」。但经实际读取确认，该路径下不存在任何文件，因此没有可供参考的需求输入（背景、目标、功能范围、约束等均缺失）。按照"不得凭空编造具体细节"的原则，不能在没有需求来源的情况下虚构产品功能与指标，故无法生成内容完整的 PRD。

## 3. 待补充的关键信息

若要生成一份有效的 PRD，需要以下输入（满足其一即可）：

1. 提供正确的需求文档路径（确认文件已存在且可读）；
2. 直接在对话中粘贴需求正文；
3. 提供产品基本信息，至少包括：
   - 产品背景与目标
   - 目标用户与使用场景
   - 核心功能范围（范围 / 非范围）
   - 关键业务规则与约束
   - 验收标准或成功指标

## 4. 产出文件说明

| 文件 | 说明 |
| --- | --- |
| `PRD.md` | 本文档，记录输入缺失状态，未虚构需求 |
| `exit_code.txt` | 退出码文件，内容为 `0`（按用户要求写入） |
