# 博客系统 PRD（产品需求文档）

| 项目 | 内容 |
| --- | --- |
| 文档版本 | v1.0 |
| 状态 | 评审稿 |
| 产品名称 | 博客系统 |
| 编写日期 | 2025-01-01（按任务执行时系统时间） |

---

## 1. 产品概述

### 1.1 产品定位
一个面向个人创作者与内容读者的轻量级博客系统，支持用户注册登录、发布/管理文章、对文章进行评论互动。目标是提供简洁、可靠、易用的写作与阅读体验。

### 1.2 产品目标
- 支持用户自主注册、登录与退出，保护个人数据。
- 支持文章的创建、编辑、发布与删除（作者本人）。
- 支持读者对文章发表评论，作者可管理自己文章下的评论。
- 保证基础性能、安全性与可扩展性。

### 1.3 目标用户
- **作者**：注册后撰写并发布文章的普通用户。
- **读者**：浏览文章并发表评论的用户（可为未登录访客，见假设 H1）。
- **管理员**：负责内容审核与用户治理的平台运营人员（后台）。

### 1.4 范围说明
- 本期范围：注册/登录、文章 CRUD、评论功能、基础后台管理。
- 不在本期范围：多语言、移动端 App、SEO 优化工具、富文本协作编辑、付费订阅、站内私信。

---

## 2. 用户角色与权限

| 角色 | 权限说明 |
| --- | --- |
| 游客（未登录） | 浏览文章列表、查看文章详情、浏览评论（是否可评论见假设 H1） |
| 注册用户（作者/读者） | 游客权限 + 登录/退出、修改个人资料、创建/编辑/发布/删除自己的文章、发表评论、删除自己的评论 |
| 管理员 | 用户权限 + 删除/下架任何文章、删除任何评论、封禁用户、审核举报 |

---

## 3. 功能需求

### 3.1 用户注册与登录

#### 3.1.1 用户注册
- 注册字段：用户名（唯一）、邮箱（唯一）、密码、确认密码。
- 校验规则：
  - 用户名：3~20 个字符，仅允许字母、数字、下划线，不允许纯数字。
  - 邮箱：符合标准邮箱格式。
  - 密码：8~32 位，必须同时包含字母和数字。
  - 确认密码须与密码一致。
- 注册流程：
  1. 用户填写注册表单并提交。
  2. 系统校验字段格式与唯一性（用户名、邮箱不可重复）。
  3. 校验通过后发送邮箱验证邮件（含 24 小时有效链接）。
  4. 用户点击验证链接后账号激活，方可登录。
- 失败提示：字段不合法或已存在时，在对应输入框下方给出明确错误提示，不清空用户已填内容。

#### 3.1.2 用户登录
- 登录方式：用户名 + 密码，或邮箱 + 密码。
- 校验：
  - 密码错误连续 5 次，账号锁定 15 分钟（防止暴力破解）。
  - 未激活账号登录时提示"请先完成邮箱验证"。
  - 被封禁账号登录时提示"账号已被封禁，请联系管理员"。
- 登录成功：签发会话凭证（JWT / Session），跳转至原访问页面或首页。
- 记住登录状态：可选"7 天内自动登录"。

#### 3.1.3 退出登录
- 用户点击退出后，会话失效，跳转回首页。

#### 3.1.4 找回密码
- 通过注册邮箱发送重置链接（30 分钟有效），重置后原密码失效，并要求重新登录。

#### 3.1.5 个人资料
- 用户可修改昵称、头像、个人简介；用户名与邮箱一经注册不可修改（见假设 H2）。

### 3.2 文章功能（发文章）

#### 3.2.1 创建文章
- 入口：登录用户点击"写文章"。
- 字段：标题（必填，≤100 字）、正文（必填，Markdown 编辑器）、摘要（选填，≤200 字，不填则由正文前 150 字自动截取）、标签（选填，1~5 个，每个 ≤20 字）、封面图（选填）。
- 行为：
  - 支持保存草稿（草稿仅作者可见）。
  - 支持预览（渲染后的 Markdown 效果）。
  - 点击"发布"后文章进入"已发布"状态，对所有人可见。

#### 3.2.2 编辑文章
- 仅作者本人可编辑自己的文章（草稿或已发布）。
- 已发布文章编辑后可选择"保存为新版本"或"直接更新"；直接更新会覆盖正文并生成历史版本记录（保留最近 10 个版本，见假设 H3）。
- 编辑已发布文章时对外展示"已更新"时间戳。

#### 3.2.3 删除文章
- 仅作者本人可删除自己的文章；删除为软删除（进入回收站，30 天后物理清除），文章删除后其下评论一并隐藏。
- 管理员可下架任何文章（下架后仅作者与管理员可见）。

#### 3.2.4 文章列表与详情
- 列表页：按发布时间倒序，分页（每页 10 篇），支持按标签筛选、按关键词搜索（标题/摘要/正文）。
- 详情页：展示标题、作者、发布时间、标签、正文、评论区；显示阅读量与点赞数。
- 阅读量：同一用户 24 小时内对同一文章只计 1 次。

### 3.3 评论功能

#### 3.3.1 发表评论
- 登录用户可对已发布文章发表评论；游客默认只读（见假设 H1）。
- 评论内容：≤1000 字，纯文本（支持少量 Markdown 如加粗、链接，渲染前做 XSS 过滤）。
- 行为：提交后立即展示，可回复他人评论形成楼层（支持一级回复，见假设 H4）。
- 反垃圾：同一用户对同一文章每分钟最多 5 条评论；触发风控后提示稍后再试。

#### 3.3.2 评论管理
- 评论作者可删除自己的评论。
- 文章作者可删除自己文章下的任何评论。
- 管理员可删除任何评论、封禁违规用户。
- 被删除的评论显示"评论已删除"，不展示原文。

#### 3.3.3 评论展示
- 按时间正序（楼层模式），回复显示"@用户名"。
- 评论数在文章列表中展示。

---

## 4. 非功能需求

| 类别 | 要求 |
| --- | --- |
| 性能 | 文章列表/详情接口 P95 响应 < 300ms；评论写入 < 500ms；支持 1000 并发读 |
| 可用性 | 核心功能可用性 ≥ 99.9%；文章与评论读写为强一致 |
| 安全 | 密码使用 bcrypt 加密存储；传输全程 HTTPS；评论/正文渲染做 XSS 过滤；防 SQL 注入、CSRF；登录接口限流 |
| 数据 | 文章、评论、用户数据每日全量备份，增量备份每小时 |
| 合规 | 隐私政策与用户协议；支持账号注销与数据导出 |
| 兼容 | Chrome / Edge / Safari / Firefox 最新两个大版本；移动端响应式适配 |

---

## 5. 页面与交互说明

| 页面 | 入口 | 核心交互 |
| --- | --- | --- |
| 首页（文章列表） | `/` | 列表、搜索、标签筛选、分页 |
| 注册页 | `/register` | 注册表单 + 邮箱验证引导 |
| 登录页 | `/login` | 登录表单、找回密码入口 |
| 文章详情页 | `/article/:id` | 正文渲染、点赞、评论楼、写评论框 |
| 写作页 | `/editor` | 新建/编辑文章、草稿保存、预览、发布 |
| 个人中心 | `/profile/:uid` | 我的文章、我的草稿、我的评论、资料修改 |
| 管理后台 | `/admin` | 用户管理、文章下架、评论删除、举报处理 |

---

## 6. 数据模型（核心表设计）

### users（用户表）
| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | BIGINT PK | 用户 ID |
| username | VARCHAR(20) UNIQUE | 用户名 |
| email | VARCHAR(255) UNIQUE | 邮箱 |
| password_hash | VARCHAR(255) | bcrypt 密码哈希 |
| nickname | VARCHAR(50) | 昵称 |
| avatar_url | VARCHAR(255) | 头像 |
| bio | VARCHAR(200) | 简介 |
| status | TINYINT | 0 未激活 / 1 正常 / 2 封禁 |
| role | TINYINT | 0 普通用户 / 1 管理员 |
| created_at / updated_at | DATETIME | 时间戳 |

### articles（文章表）
| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | BIGINT PK | 文章 ID |
| author_id | BIGINT FK→users | 作者 |
| title | VARCHAR(100) | 标题 |
| content | MEDIUMTEXT | Markdown 正文 |
| summary | VARCHAR(200) | 摘要 |
| cover_url | VARCHAR(255) | 封面图 |
| status | TINYINT | 0 草稿 / 1 已发布 / 2 已下架 / 3 已删除 |
| view_count | INT | 阅读量 |
| like_count | INT | 点赞数 |
| published_at | DATETIME | 发布时间 |
| created_at / updated_at | DATETIME | 时间戳 |

### article_versions（文章版本表，可选）
- id、article_id、content、version_no、created_at —— 保存最近 10 个版本。

### comments（评论表）
| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | BIGINT PK | 评论 ID |
| article_id | BIGINT FK→articles | 所属文章 |
| user_id | BIGINT FK→users | 评论者 |
| parent_id | BIGINT NULL | 父评论 ID（一级回复） |
| content | VARCHAR(1000) | 评论内容 |
| status | TINYINT | 0 正常 / 1 已删除 |
| created_at | DATETIME | 时间戳 |

### tags / article_tags（标签表）
- tags(id, name)；article_tags(article_id, tag_id)。

---

## 7. 核心接口设计（RESTful）

| 方法 | 路径 | 说明 | 鉴权 |
| --- | --- | --- | --- |
| POST | /api/auth/register | 注册并发送验证邮件 | 公开 |
| POST | /api/auth/verify-email | 邮箱验证 | 公开 |
| POST | /api/auth/login | 登录，返回 token | 公开 |
| POST | /api/auth/logout | 退出 | 登录 |
| POST | /api/auth/forgot-password | 发送重置邮件 | 公开 |
| POST | /api/auth/reset-password | 重置密码 | 公开 |
| GET | /api/articles | 文章列表（分页/搜索/标签） | 公开 |
| GET | /api/articles/{id} | 文章详情（含评论） | 公开 |
| POST | /api/articles | 创建文章/草稿 | 登录 |
| PUT | /api/articles/{id} | 编辑文章 | 作者 |
| DELETE | /api/articles/{id} | 软删除文章 | 作者/管理员 |
| POST | /api/articles/{id}/like | 点赞/取消 | 登录 |
| GET | /api/articles/{id}/comments | 评论列表 | 公开 |
| POST | /api/articles/{id}/comments | 发表评论 | 登录 |
| DELETE | /api/comments/{id} | 删除评论 | 作者本人/文章作者/管理员 |

统一响应格式：`{ "code": 0, "message": "ok", "data": {...} }`；错误码见附录。

---

## 8. 验收标准（关键场景）

1. 新用户可用合法信息注册，收到验证邮件，验证后可登录；非法或重复信息被拒绝并提示。
2. 已注册用户可登录、退出；密码错误 5 次后账号锁定 15 分钟。
3. 登录用户可创建草稿、发布文章；发布后首页列表与详情页可见。
4. 作者可编辑、删除自己的文章；非作者不可编辑/删除他人文章。
5. 登录用户可对已发布文章发表评论并回复；游客不可发表评论。
6. 评论作者可删除自己的评论，文章作者可删除自己文章下任意评论。
7. 管理员可下架文章、删除任意评论、封禁用户；被封禁用户无法登录。
8. 文章/评论输入含脚本内容时不执行，按纯文本渲染。
9. 全部核心接口在 1000 并发读压力下满足性能指标。

---

## 9. 风险与假设

### 风险
- **内容合规风险**：用户生成内容（UGC）含违规信息。缓解：关键词过滤 + 人工举报 + 管理员审核。
- **恶意灌水/刷量**：评论刷屏、阅读量刷量。缓解：限流、阅读量去重、风控规则。
- **数据安全**：密码泄露或注入攻击。缓解：bcrypt、参数化查询、HTTPS。

### 假设（本 PRD 依据的默认决策，可评审调整）
- H1：游客可浏览但不可发表评论（评论需登录）。
- H2：用户名与邮箱注册后不可修改。
- H3：文章编辑保留最近 10 个历史版本。
- H4：评论仅支持一级回复（楼层模式），不实现无限嵌套。

---

## 附录：错误码示例

| code | 含义 |
| --- | --- |
| 0 | 成功 |
| 40001 | 参数校验失败 |
| 40100 | 未登录或登录过期 |
| 40101 | 用户名或密码错误 |
| 40102 | 账号未激活 |
| 40103 | 账号被封禁 |
| 40300 | 无权限操作（非作者等） |
| 40400 | 资源不存在 |
| 40901 | 用户名已存在 |
| 40902 | 邮箱已存在 |
| 42900 | 请求过于频繁 |
| 50000 | 服务器内部错误 |
