# PandaPal Desktop — 环境变量模板
# 复制此文件为 .env.development（开发）或 .env.production（生产），填入真实值
#
# 开发时客户端走相对路径（/auth），由 vite.config.ts 的 proxy 转发到云端服务器：
#   Tauri WebView 请求 localhost:5173/auth/login → Vite 做服务端转发，
#   绕过浏览器 CORS 限制（服务端→服务端无 CORS）。
# 生产环境客户端直连完整 URL。
#
# 注意：服务器地址只允许出现在 env 文件中（.env* 已被 .gitignore 忽略），
#       禁止硬编码进源码（vite.config.ts / src/）。

# Relay 服务器认证 API 地址（完整 URL，含 /auth 路径）
# - 开发：Vite dev proxy 从它推导转发目标（去掉尾部 /auth），客户端请求走相对路径
# - 生产：客户端直连该地址
VITE_RELAY_AUTH_URL=https://your-relay-domain.com/auth
