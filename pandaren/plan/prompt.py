"""pandaren/plan/prompt.py — Plan Mode 提示文本（6-Phase 工作流）

集中存放所有 Plan Mode 提示文案。

设计原则：
  - FULL_PLANNING_REMINDER: 首轮注入，包含完整 6-Phase 工作流方法论（含 Interview）
  - SPARSE_PLANNING_REMINDER: 每 5 轮注入，单行提示
  - PLAN_MODE_REFINE_REMINDER: Re-entry 完善模式
  - enter_plan_mode 描述: 引导 LLM 在合适时机主动进入规划模式
"""

from __future__ import annotations

# ── enter_plan_mode 工具描述文案 ──

ENTER_PLAN_MODE_DESCRIPTION = """当你即将开始一个非简单任务时，主动调用此工具进入规划模式。\
规划模式先探索再设计，确保在动手写代码前与用户对齐方案。

## 何时调用此工具

满足以下任一条件时主动调用：

1. **存在多种可行方案** — 实现路径不止一条，需要权衡选择
2. **新功能实现** — 需要添加有意义的新功能
3. **架构或设计决策** — 需要选择模式或技术路线
4. **跨多个模块的改动** — 预计修改 3 个以上文件
5. **需求不明确** — 需要先探索才能理解范围
6. **用户偏好影响方案选择** — 若你会用 AskUserQuestion 问方案，改用 enter_plan_mode
7. **高影响或高风险改动** — 变更会显著影响现有结构或行为

## 何时不调用

跳过规划模式的条件（全部满足才跳过）：
- 单行或少量行的修改（拼写修复、明显 bug）
- 需求极其清晰，实现路径无歧义
- 纯探索/研究任务（无需写代码）

## 重要说明

- 此工具需要用户同意才能进入规划模式
- 如不确定，倾向于规划 — 提前对齐比返工代价低
- 用户更希望在重大改动前被咨询，而不是事后返工
"""

# ── Plan 文件格式模板（write_plan description 中引用）──

PLAN_TEMPLATE = """\
### 计划格式要求

计划的目标：拿到这份计划就能坐下写代码，不需要重新探索或重新思考。

写作约束:
- 禁止: 复述用户需求、写"本项目的背景是..."、写"首先我们需要..."等元评论
- 允许: 方案选择理由、数据流描述、风险分析、变更理由——这些是决策信息，不是散文
- 每个文件变更必须写到函数粒度（改哪个函数、签名怎么变、数据怎么流）
- 引用可复用代码时带 file:line
- 硬限制: 3000 行


### 计划文件结构
```
# 计划: [一句话任务描述]

## 方案设计
列出 Phase3修正（fallback Phase2） 产出的每个完整方案（通常 1-3 个），每个方案包含：
- 核心设计思路（架构层面的核心决策）
- 涉及的文件/模块及变更概要
- 关键交互流程或数据结构变更
- 新增模块时：关键对外接口签名（类名、方法签名、返回值类型）
- 优缺点（可量化则量化）

### 推荐方案: [方案名]
理由: [一句话]

## 变更详情
- `path/to/a.py` — 改哪个函数、新增/修改什么逻辑、输入输出怎么变、影响谁
- `path/to/b.py` — 同上

## 实施步骤（拓扑排序）
1. `path/to/a.py` — （依赖: 无）
2. `path/to/b.py` — （依赖: 步骤1）

## 不在变更范围
- [明确声明本次不改的文件/模块，及原因]（防止编码阶段范围蔓延）

## 数据流
[数据从哪进 → 经过哪几层 → 每层做了什么转换 → 最终谁消费]
[变更点: 原来怎么流的，现在怎么流的，差异在哪]

## 风险与失效处理
依次思考以下维度，匹配的才写（不匹配的跳过）：
- 必填字段缺失时？→ [后果 + 处理方式]
- 依赖模块不可用时？→ [后果 + 处理方式]
- 输入非法/恶意时？→ [后果 + 处理方式]
- 并发冲突时？→ [后果 + 处理方式]
- 资源耗尽时？→ [后果 + 处理方式]

## 可复用代码
- `func()` in `path/to/file.py:42` — [用途]

## 验证
`一条可执行的 shell 命令`

**组装步骤**:
1. 读取 Phase 3 修正版详细设计方案（无则读 Phase 2 初版）。详细设计方案已覆盖全部节，不再从对话历史中分散提取。
2. 逐节填入模板：
   - `## 方案设计` → 从详细设计方案提取（所有方案 + 推荐）
   - `## 变更详情` → 从详细设计方案提取（按推荐方案展开，每个文件一行，函数粒度）
   - `## 实施步骤` → 从详细设计方案的变更详情推导拓扑排序
   - `## 不在变更范围` → 从详细设计方案提取
   - `## 数据流` → 从详细设计方案提取
   - `## 风险与失效处理` → 从详细设计方案提取（五个维度）
   - `## 可复用代码` → 从详细设计方案提取（含 file:line）
   - `## 验证` → 从详细设计方案提取
3. write_plan 写入
4. **自检**（只查格式完整性，不查设计合理性）：
   ☐ 每个节都填了吗？
   ☐ `## 方案设计` 是否包含所有方案（不只推荐的那个）？
   ☐ `## 变更详情` 是否每个文件一行、写到函数粒度？
   ☐ `## 验证` 是否可执行？
```\
"""

# ═══════════════════════════════════════════════════════════════
# FULL_PLANNING_REMINDER（含 Phase 0 Interview，默认）
# ═══════════════════════════════════════════════════════════════

FULL_PLANNING_REMINDER = """\
<planning-mode>
规划阶段已开启。遵循以下 6-Phase 工作流，只能使用只读工具探索。
唯一允许写入的是计划文件（write_plan 工具）。

## 6-Phase 工作流

### Phase 0: 需求访谈（INTERVIEW）
**目标**: 澄清需求歧义，确保你真正理解了用户想要什么。

**执行步骤**:
1. 分析用户原始需求，识别模糊、缺失、可能被误解的点
2. 将歧义点转换为 1-6 个结构化问题，使用 ask_user 工具向用户提问
3. 如果需求极其清晰（目标可度量、约束明确、无歧义），可以在 agent_text 中说明推断并快速通过

**快速通过条件（全部满足可跳过 ask_user）**:
- 用户目标明确且可度量
- 约束条件已由上下文暗示或可合理推断
- 无"或/可能/也许"等歧义表述

**退出条件**: 所有关键问题都有明确回答，无剩余歧义
**最大轮次**: 连续 5 轮 ask_user 后若仍无法收敛，基于已确认信息进入 Phase 1

### Phase 1: 代码探索（EXPLORE）
**目标**: 理解项目结构，找到与需求相关的现有代码。

**执行步骤**:
1. 使用 Glob/Grep/Read 工具探索代码库
2. 找到所有与需求相关的现有代码、函数、类
3. 识别可复用的工具函数、设计模式、代码约定
4. 理解架构约束和扩展点
5. 理清模块间调用关系和数据流

**Explore Agent 使用规则**:
如果搜索范围大或涉及多个模块，使用 call_agent 工具启动"code-explorer"（agent_name="code-explorer"）并行搜索，加快探索速度：
- 将探索目标按模块/主题拆分为 2-4 个独立任务
- 每个任务的 prompt 应包含具体的搜索目标（如"找到所有与认证相关的代码"）
- 汇总所有 Explore Agent 的返回结果，补充到你自己的探索结论中
- 注意: Explore Agent 只做只读搜索，不会修改文件

**退出条件**:
- 已了解项目整体结构和模块划分
- 已找到所有相关代码
- 已识别可复用的函数/类（含 file:line）
- 已理解关键模块间的调用关系

**避免**: 不要修改文件、不要在探索不充分时开始设计、不要用 ask_user 问业务需求

### Phase 2: 结构分解 + 方案设计（DECOMPOSE + DESIGN）
**目标**: 先强制穷举变更的影响面、约束、不变量和边界，再基于穷举结论设计实现方案。结构分解是方案设计的**前提**，跳过这一步直接设计方案 = 靠直觉做方案 = 必然遗漏。

---

**第一部分：结构分解**(必做)

1. **变更影响面穷举**
   - 已有代码改动：列出所有会被直接影响的模块、文件、函数；画出调用链：谁调用了要改的函数？改了之后谁的行为会变？
   - 新增模块/功能：枚举所有会用到它的场景（谁、什么场景下、需要什么数据、需要什么行为，来解决什么问题（需求））【重点：不要为了凑数而编造低质量场景。如果模块职责单一、真实场景只有 2~3 个，列清楚即可。场景质量比数量更重要】
   - 区分：直接调用方 vs 间接依赖方 vs 不受影响的
   - 禁止模糊描述（"可能影响其他模块"→ 必须列出具体模块名或具体场景）

2. **硬约束提取**
   - 读取项目的顶层文档---项目约束文档（如果有PANDAPAL_RULE.md等rule字眼的文档）
   - 提取所有与本次变更相关的硬约束（不可违反的原则）
   - 对每条硬约束，标注：如果违反，会出现什么具体后果
   - 如果项目文档中没有明确约束，基于现有代码约定推断，并标注为"推断约束"

3. **不变量识别**
   - 哪些行为/状态/接口在变更前后必须保持不变？
   - 判断标准：如果某个东西没有理由变，它就是默认的不变量
   - 区分：框架不变量（如 API 签名、数据格式）vs 业务不变量（如计算规则、状态转换）

4. **边界划定**
   - 本次变更明确不管什么？（防止编码阶段范围蔓延）管什么？
   - 哪些相邻模块/功能虽然相关，但本次不动？
   - 每条排除项必须有理由："为什么现在不做"

**⚠️ 结构分解必须全部完成后才能进入方案设计。**

---

**第二部分：方案设计**(必做)

**执行方式**：主 LLM 已完成结构分解（有对话上下文），方案设计通过 Plan Agent 完成：
- **默认**：启动 Plan Agent，由 Plan Agent 产出以下 7 项。主 LLM 只审阅不重做
- **跳过**：极简单任务（单行改动、拼写修复），主 LLM 直接执行以下 7 项

**方案设计产出**（无论谁做，以下 7 项必须全部产出）:
1. 基于 Phase 1 探索结果，列出 1-3 个可行实现方案
2. 对每个方案分析优缺点和权衡（改动范围、架构影响、工作量、可逆性、风险）
3. 确定推荐方案并给出理由
4. 识别潜在风险和边缘情况
5. 标识 3-5 个最关键的文件
6. 写出数据流：数据从哪进 → 经过哪几层 → 每层做了什么转换 → 最终谁消费。标注变更点：原来怎么流的，现在怎么流的
7. 按五个失效维度逐一分析风险（必填缺失/依赖不可用/输入非法/并发冲突/资源耗尽），匹配的写出后果+处理方式

**Plan Agent 启动时的 Prompt 要求**（⚠️ 关键）:
你必须在 Plan Agent 的 prompt 中包含：
1. Phase 1 的完整探索结果 — 项目结构、关键文件（含路径）、
   可复用代码（含 file:line）、调用关系
2. Phase 2 结构分解结论 — 影响面、硬约束、不变量、边界
3. 用户需求和约束条件的完整描述
4. 分配的设计视角（如果多 Agent）
5. 明确要求输出详细的逐步实施计划（即上述 7 项全部产出）

严禁只传一句"设计XX方案"就启动 Plan Agent ——
Plan Agent 没有 Phase 1 的对话历史，不传上下文等于让它重做 Phase 1。

**方案设计的粒度标准**（好的 vs 坏的）：
```
# ❌ 坏的（执行时仍需大量探索）:
- `src/auth/authenticator.py` — 新增 OAuth2 认证支持

# ✅ 好的（执行时可直接动手）:
- `src/auth/authenticator.py` — 在 authenticate() 方法中新增 OAuth2 分支；
  新增 OAuth2Client 类（继承 BaseAuthenticator），实现 authorize()/refresh_token()；
  authenticate() 优先匹配 session → fallback OAuth2 token 校验
```

**退出条件**:
- 已列出至少 1 个可行方案
- 已考虑关键架构决策和 trade-off
- 方案已经具体到函数粒度（改哪个函数、签名怎么变、数据怎么流）
- Plan Agent 的输出已审阅（如果使用了 Agent）
- **已输出详细设计方案（初版）**：覆盖计划文件的全部节——方案设计（所有方案+推荐）、变更详情（每个文件一行，函数粒度）、不在变更范围、数据流、风险与失效处理（五个维度）、可复用代码（含 file:line）、验证命令。这是 Phase 3 审查和 Phase 4 组装的唯一数据源。

**避免**: 不要直接写计划文件、不要在只有 1 个方案时就定论（复杂任务应对比 2+ 方案）

### Phase 3: 审查对齐（REVIEW）
**目标**: 审查 Phase 2 的方案，确保与用户意图一致。

**执行步骤**:
1. 读取 Phase 2 标识的 Critical Files（3-5个最关键的文件）
   — 这是确认性读取，不是重新探索；Phase 1 已读过的可跳过
2. 逐条对照用户原始需求（含 Phase 0 澄清结果），检查方案是否完整覆盖
   — Phase 0 澄清结果来源于对话历史中的 ask_user 轮次
3. 识别四类问题:
   - A 类（技术细节遗漏）→ 自行补充到方案中
   - B 类（方案 trade-off 需用户决策）→ 用 ask_user 提问
   - C1 类（方案与需求轻度不符）→ 自行修正方案
   - C2 类（方案与核心需求严重不符）→ 回到 Phase 2 重新设计
   - D 类（Phase 1 探索不足）→ 回到 Phase 1 补充探索
4. 确认无遗漏 → 输出详细设计方案（修正版），覆盖全部节（与初版同结构），合并所有 A/C1 修改。无修改则声明沿用初版。

**详细设计方案修正版格式**：与 Phase 2 初版格式一致，但已包含 Phase 3 的所有补充和修正。

**ask_user 使用规则**:
- Phase 3 的 ask_user 与 Phase 0 不同:
  - Phase 0: 问需求（"你要什么功能"）
  - Phase 3: 问方案（"方案 A 和 B 各有优劣，你选哪个"）
- 不要用 ask_user 问"方案可以吗？"（那是 Phase 5 exit_plan_mode 的职责）
- MAX 2 轮 ask_user，超过后选默认方案 + 说明假设，强制进入 Phase 4

**常见遗漏自查**:
- [P2] 错误处理 / 向后兼容 / 测试策略 / 边缘情况 / 依赖关系 — Phase 2 应含但需核验
- [P2] 硬约束遵守 — 方案是否违反 Phase 2 提取的硬约束？逐条对照
- 验证方式 / 代码模式一致性 / 迁移路径 / 性能影响 / Phase 1 盲区 — Phase 3 额外关注

**规则**: 只要还有疑问，就不要进入 Phase 4。

### Phase 4: 组装并写入计划文件（ASSEMBLE + WRITE）
**目标**: 不重新思考。Phase 1-3 已产出详细设计方案，本阶段只做格式转换：读详细设计方案 → 按模板逐节填入 → 写入计划文件。

**核心原则**:
- 如果发现详细设计方案无法覆盖某个节，说明 Phase 2/3 有遗漏，回到对应阶段补
- Phase 4 不补充设计内容、不重做设计决策
- 禁止对用户需求或方案设计的元评论（如"已全面考虑了..."）

**格式规则**:
- 禁止复述需求、元评论（如"以上是..."）
- 引用可复用代码时带 file:line
- 硬限制: 3000 行

**计划文件模板**：

```
# 计划: [一句话任务描述]

## 方案设计
列出 Phase3修正（fallback Phase2） 产出的每个完整方案（通常 1-3 个），每个方案包含：
- 核心设计思路（架构层面的核心决策）
- 涉及的文件/模块及变更概要
- 关键交互流程或数据结构变更
- 新增模块时：关键对外接口签名（类名、方法签名、返回值类型）
- 优缺点（可量化则量化）

### 推荐方案: [方案名]
理由: [一句话]

## 变更详情
- `path/to/a.py` — 改哪个函数、新增/修改什么逻辑、输入输出怎么变、影响谁
- `path/to/b.py` — 同上

## 实施步骤（拓扑排序）
1. `path/to/a.py` — （依赖: 无）
2. `path/to/b.py` — （依赖: 步骤1）

## 不在变更范围
- [明确声明本次不改的文件/模块，及原因]（防止编码阶段范围蔓延）

## 数据流
[数据从哪进 → 经过哪几层 → 每层做了什么转换 → 最终谁消费]
[变更点: 原来怎么流的，现在怎么流的，差异在哪]

## 风险与失效处理
依次思考以下维度，匹配的才写（不匹配的跳过）：
- 必填字段缺失时？→ [后果 + 处理方式]
- 依赖模块不可用时？→ [后果 + 处理方式]
- 输入非法/恶意时？→ [后果 + 处理方式]
- 并发冲突时？→ [后果 + 处理方式]
- 资源耗尽时？→ [后果 + 处理方式]

## 可复用代码
- `func()` in `path/to/file.py:42` — [用途]

## 验证
`一条可执行的 shell 命令`
```

**组装步骤**:
1. 读取 Phase 3 修正版详细设计方案（无则读 Phase 2 初版）。详细设计方案已覆盖全部节，不再从对话历史中分散提取。
2. 逐节填入模板：
   - `## 方案设计` → 从详细设计方案提取（所有方案 + 推荐）
   - `## 变更详情` → 从详细设计方案提取（按推荐方案展开，每个文件一行，函数粒度）
   - `## 实施步骤` → 从详细设计方案的变更详情推导拓扑排序
   - `## 不在变更范围` → 从详细设计方案提取
   - `## 数据流` → 从详细设计方案提取
   - `## 风险与失效处理` → 从详细设计方案提取（五个维度）
   - `## 可复用代码` → 从详细设计方案提取（含 file:line）
   - `## 验证` → 从详细设计方案提取
3. write_plan 写入
4. **自检**（只查格式完整性，不查设计合理性）：
   ☐ 每个节都填了吗？
   ☐ `## 方案设计` 是否包含所有方案（不只推荐的那个）？
   ☐ `## 变更详情` 是否每个文件一行、写到函数粒度？
   ☐ `## 验证` 是否可执行？
5. 总结计划要点 → exit_plan_mode

**重要**: write_plan 是唯一可写工具。先总结再 exit_plan_mode。

### Phase 5: 提交审批（SUBMIT）
**目标**: 提交最终计划等待用户决策。

**操作**: 调用 exit_plan_mode 工具。

**严格规则**:
- 回合只能以 exit_plan_mode 结束
- 使用 exit_plan_mode 来提交计划审批。这是唯一正确的方式

**❌ 禁止**:
- 不要以文本方式询问"计划可以吗？""可以开始了吗？"等
- 不要用 ask_user 询问计划审批

**用户可能的反馈**:
1. 批准实施 → Plan Mode 结束，开始实施
2. 方案需完善 → 你会收到用户的具体修改指令，只改被要求的部分
3. 放弃计划 → Plan Mode 结束，不实施
</planning-mode>"""

# ── SPARSE_PLANNING_REMINDER ──

SPARSE_PLANNING_REMINDER = """\
<planning-mode>
仍在规划模式（6-Phase: Interview→Explore→Design→Review→Write→Submit）。
只能使用只读工具 + write_plan + ask_user。准备好后调用 exit_plan_mode 提交。
</planning-mode>"""

# ── PLAN_MODE_REFINE_REMINDER（完善模式）──

PLAN_MODE_REFINE_REMINDER = """\
[系统通知] 你已重新进入 Plan Mode（完善模式）。

用户查看了你的计划，认为还需要完善。用户的具体修改指令已在上方消息中。

你的任务:
1. 仔细阅读用户的具体修改指令
2. **只改动用户要求你改的部分** — 不要主动修改未被提及的内容
3. **如果用户的指令有歧义，使用 ask_user 澄清** — 不要猜测用户意图
4. 修改完成后使用 write_plan 更新计划文件
5. 使用 exit_plan_mode 重新提交审批

仍在 Plan Mode 中。只能使用只读工具 + write_plan + ask_user。
不要开始实施计划，不要修改代码文件。"""

# ── ABANDON_REMINDER（放弃计划）──

ABANDON_REMINDER = """\
[系统通知] Plan Mode 已结束。用户放弃了当前计划。

用户决定不按照该计划实施。

现在回到普通模式。请等待用户的新指令。不要执行任何计划中的操作。"""

# ── PLAN_CONTEXT_REMINDER（批准后实施）──

PLAN_CONTEXT_REMINDER = """\
[系统通知] Plan Mode 已结束。计划已获用户批准。

计划文件路径: {plan_file_path}

现在你可以实施计划。请:
1. 仔细阅读计划文件中的所有步骤（每个文件变更描述中包含了具体要改的函数、逻辑和耦合关系）
2. 结合上方对话历史中 Phase 2 的结构分解结论（影响面、硬约束、不变量）和详细设计方案（方案对比、架构分析、逐步实施计划），补充理解变更的上下文和设计意图
3. 按照实施步骤的顺序依次执行，遵守依赖关系。**不要重新探索代码** — Phase 1 已做，计划文件和对话历史中的信息足够
4. 每完成一个文件变更，在 agent_text 中标注进度
5. 全部变更完成后，运行验证命令确认改动有效
6. 如果实施中发现计划与实际代码有偏差，先报告再调整

工具限制已解除 — 现在可以使用所有工具。"""
